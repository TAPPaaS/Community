# wiim-fleet — Wiim Pro Plus audio streamer (DEFERRED)

Policy-only TAPPaaS module placeholder for the Wiim Pro Plus audio
streamer.

## Status

**Deferred.** Device exists (DHCP reservation `wiimproplus-9786` at
192.168.40.17 pre-migration), but no current pfSense rule references it.
Registered for inventory; will gain `provides` + `services/<svc>/
pinhole.json` when Home Assistant integration is added.

## Out of scope

- No VM. No current cross-zone consumer.
